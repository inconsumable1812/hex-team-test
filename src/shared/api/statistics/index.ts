import { fetch as fetchStatistics } from './fetch';
import {
  QueryParameters as StatisticsQueryParameters,
  Response as StatisticsResponse
} from './types';

export type { StatisticsQueryParameters, StatisticsResponse };

export { fetchStatistics };
