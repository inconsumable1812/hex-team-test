import { fetch as fetchLogin } from './fetch';
import {
  QueryParameters as LoginQueryParameters,
  Response as LoginResponse
} from './types';

export type { LoginQueryParameters, LoginResponse };

export { fetchLogin };
