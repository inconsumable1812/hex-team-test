import { fetch as fetchRegister } from './fetch';
import {
  QueryParameters as RegisterQueryParameters,
  Response as RegisterResponse
} from './types';

export type { RegisterQueryParameters, RegisterResponse };

export { fetchRegister };
